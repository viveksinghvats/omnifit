import 'package:flutter/material.dart';

drawerScreen(context) => SizedBox(
      width: MediaQuery.of(context).size.width * 0.5,
      child: Drawer(
        child:Column(children: [
          Container(
            
          )
        ],),
      ),
    );
